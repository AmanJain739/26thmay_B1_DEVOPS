1.Create a directory new at location /mnt
<$ sudo -i>
<#root - mkdir /mnt/new>

2.Create a partition and mount it on /mnt/new
<$sudo -i>
<#root-fdisk /dev/xvdf>
<n>        //for new partition
enter    //partition type
enter    // 1-4 (no)
enter    // first sector
<+500M> // last sector 
<p>        // for printing the created partition
<w>      // save 
<#root- mkfs.xfs  /dev/xvdf>
<#root- mount  /dev/xvdf1/mnt/new>
<#root- df-hT> 

3.Install apache web server
<$ sudo apt install apache2>
<$ sudo systemctl status apache2>

4.Change apache web serevr root directory path  /var/www/html  to /mnt/new
<root#- cd /var/www>
<root#-ls>
<root#-cd html/>
<root#-ls>
<root#-cd /etc/apache2/sites-available>
<root#-ls>
<root#-vim 000-defualt.conf>
// change the document root  to /mnt/new
//wq! - to save
<root#-systemctl restart apache2>
